"""Retired duplicate source module.

The canonical implementation lives in the category that owns the tested parser.
This module intentionally declares no source class, preventing silent duplicate-ID
overrides during plug-in discovery.
"""
